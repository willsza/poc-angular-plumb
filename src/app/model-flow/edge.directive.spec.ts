import { EdgeDirective } from './edge.directive';

describe('EdgeDirective', () => {
  it('should create an instance', () => {
    const directive = new EdgeDirective();
    expect(directive).toBeTruthy();
  });
});
